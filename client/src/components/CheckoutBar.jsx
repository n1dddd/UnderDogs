import React from 'react'
import styles from "./CheckoutBar.module.scss"

const CheckoutBar = () => {
    return (
        <div className={styles.checkoutBar}>CheckoutBar</div>
    )
}

export default CheckoutBar